import * as S from './styles';
import React from 'react';

const HorizontalScroll = ({ onPressFunc, title }) => (
  <S.HorizontalScrollContainer>

  </S.HorizontalScrollContainer>
);

export default HorizontalScroll;
